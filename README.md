# Jenkins CI Demo

This is a simple demo app for Jenkins CI pipeline.

## What it does
- Prints version, hostname, date
- Shows basic system information

## How to run

```bash
chmod +x app.sh test.sh
./app.sh
./test.sh
